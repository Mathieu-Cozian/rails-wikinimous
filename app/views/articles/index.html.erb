<div class= "container">
  <div>
  <% @articles.each do |article| %>
    <%= link_to article.title, article_path(article), id: "article-title" %>
    <%= content_tag :div, article.content, id: "article-content" %>
  <% end %>
  </div>
</div>
